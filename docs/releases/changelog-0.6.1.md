# Release 0.6.1

### Improvements

- `sandiaqscout.hybrid` device now prefers using `xCD` gate over `CD` and `yCD` to reduce gate depth (#33)

### Fixes

- Switches `JC` and `AJC` gates to use the appropriate op codes in `to_jaqal` (#33)

### Contributors

This release contains contributions from:
Jim Furches
